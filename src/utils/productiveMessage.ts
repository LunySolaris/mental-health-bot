function productiveMessage(answer: boolean): string {
  if (answer) {
    return "That is great! Keep up the good work! You are strong and amazing 💪💖";
  }
  return "Don't worry, never think of this as a step back, simply a smaller step forward, because by just having this bot you are trying 🌱";
}
